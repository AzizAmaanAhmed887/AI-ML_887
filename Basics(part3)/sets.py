s = {1, 2, 3, 4}
s2 = {4, 5, 3, 6}
print(f"intersection = {s.intersection(s2)}")  # {3}
print(f"Union = {s.union(s2)}")  # {1, 2, 3, 4, 5, 6, 7, 8}
# s.add(4)  # add element
# s.remove(2)  # remove element (error if not present)
# s.clear()         # remove all elements
# s.discard(10)     # remove (no error)

# empty_set = {}
# print(type(empty_set)) # <class 'dict'>
